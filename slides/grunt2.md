## Gruntfile.js

```
grunt.initConfig({
  concat: {
    foo: {
      // opcions de la tarea concat y el objetivo foo.
    },
    bar: {
      // opcions de la tarea concat y el objetivo bar.
    },
  },
});
```
